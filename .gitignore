# dependencies
/node_modules

# build output
/dist/
/exports/
*.zip

# generated QA material that is not published
/public/qa/extended-source-review/

# environment (Firebase client config is bundled; secrets never belong here)
.env*

# tooling noise
# .wrangler is left over from the removed Cloudflare preview stack; a stale file lock
# can prevent deletion, so it is ignored rather than committed.
/.wrangler/
.DS_Store
__pycache__/
*.py[cod]
*.tsbuildinfo
npm-debug.log*
*.log
/coverage
